\newcommand{\content}{$$
\inference[<]{\inference[<]{\inference[<]{\inference[>]{\inference{\token{The}}{\cat{NP}_{nb}/\cat{N}_{}:\lambda x.x}\inference{\token{rooms}}{\cat{N}_{}:\mathrm{room}_{0.0}}}{\cat{NP}_{nb}:\mathrm{room}_{0.0}}\inference{\inference[<]{\inference[>]{\inference{\token{were}}{(\cat{S}_{dcl} \bsl \cat{NP}_{})/(\cat{S}_{adj} \bsl \cat{NP}_{}):\lambda x.x}\inference{\token{clean}}{\cat{S}_{adj} \bsl \cat{NP}_{}:\lambda x.(x_{\circ 45.0})}}{\cat{S}_{dcl} \bsl \cat{NP}_{}:\lambda x.(x_{\circ 45.0})}\inference{\token{,}}{(\cat{S}_{dcl} \bsl \cat{NP}_{}) \bsl (\cat{S}_{dcl} \bsl \cat{NP}_{}):\lambda x.\lambda y.(x, y)}}{\cat{S}_{dcl} \bsl \cat{NP}_{}:\lambda y.(\lambda x.(x_{\circ 45.0}), y)}}{\cat{NP}_{} \bsl \cat{NP}_{}:\lambda y.(\lambda x.(x_{\circ 45.0}), y)}}{\cat{NP}_{}:\lambda x.(x_{\circ 45.0}), \mathrm{room}_{0.0}}\inference[>]{\inference{\token{and}}{(\cat{NP}_{} \bsl \cat{NP}_{})/\cat{NP}_{}:\lambda x.\lambda y.(x, y)}\inference[<]{\inference{\inference{\token{upscale}}{\cat{N}_{}:\mathrm{upscale}_{0.0}}}{\cat{NP}_{}:\mathrm{upscale}_{0.0}}\inference[>]{\inference{\token{for}}{(\cat{NP}_{} \bsl \cat{NP}_{})/\cat{NP}_{}:\lambda x.\lambda y.\mathrm{for}_{0.0}^{2}(x, y)}\inference[<]{\inference[>]{\inference{\token{the}}{\cat{NP}_{nb}/\cat{N}_{}:\lambda x.x}\inference[>]{\inference{\token{low}}{\cat{N}_{}/\cat{N}_{}:\lambda x.(x_{\circ -5.0})}\inference{\token{price}}{\cat{N}_{}:\mathrm{price}_{0.0}}}{\cat{N}_{}:\mathrm{price}_{-5.0}}}{\cat{NP}_{nb}:\mathrm{price}_{-5.0}}\inference{\inference[>B]{\inference[>T]{\inference{\token{we}}{\cat{NP}_{}:\mathrm{we}_{0.0}}}{\cat{S}_{X}/(\cat{S}_{X} \bsl \cat{NP}_{}):\lambda f.(f\;\mathrm{we}_{0.0})}\inference{\token{paid}}{(\cat{S}_{dcl} \bsl \cat{NP}_{})/\cat{NP}_{}:\lambda x.\lambda y.\mathrm{pay}_{0.0}^{0}(x, y)}}{\cat{S}_{dcl}/\cat{NP}_{}:\lambda x.\mathrm{pay}_{0.0}^{0}(x, \mathrm{we}_{0.0})}}{\cat{NP}_{} \bsl \cat{NP}_{}:\lambda x.\mathrm{pay}_{0.0}^{0}(x, \mathrm{we}_{0.0})}}{\cat{NP}_{}:\mathrm{pay}_{0.0}^{0}(\mathrm{price}_{-5.0}, \mathrm{we}_{0.0})}}{\cat{NP}_{} \bsl \cat{NP}_{}:\lambda y.\mathrm{for}_{0.0}^{2}(\mathrm{pay}_{0.0}^{0}(\mathrm{price}_{-5.0}, \mathrm{we}_{0.0}), y)}}{\cat{NP}_{}:\mathrm{for}_{0.0}^{2}(\mathrm{pay}_{0.0}^{0}(\mathrm{price}_{-5.0}, \mathrm{we}_{0.0}), \mathrm{upscale}_{0.0})}}{\cat{NP}_{} \bsl \cat{NP}_{}:\lambda y.(\mathrm{for}_{0.0}^{2}(\mathrm{pay}_{0.0}^{0}(\mathrm{price}_{-5.0}, \mathrm{we}_{0.0}), \mathrm{upscale}_{0.0}), y)}}{\cat{NP}_{}:\mathrm{for}_{0.0}^{2}(\mathrm{pay}_{0.0}^{0}(\mathrm{price}_{-5.0}, \mathrm{we}_{0.0}), \mathrm{upscale}_{0.0}), \lambda x.(x_{\circ 45.0}), \mathrm{room}_{0.0}}\inference{\token{.}}{\cat{NP}_{} \bsl \cat{NP}_{}:\lambda x.x}}{\cat{NP}_{}:\mathrm{for}_{0.0}^{2}(\mathrm{pay}_{0.0}^{0}(\mathrm{price}_{-5.0}, \mathrm{we}_{0.0}), \mathrm{upscale}_{0.0}), \lambda x.(x_{\circ 45.0}), \mathrm{room}_{0.0}}
$$}
