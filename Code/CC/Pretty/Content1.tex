\newcommand{\content}{$$
\inference[<]{\inference[<]{\inference[<]{\inference[>]{\inference{\token{The}}{\cat{NP}_{nb}/\cat{N}_{}:\lambda x.x}\inference{\token{rooms}}{\cat{N}_{}:\mathrm{room}_{0.0}}}{\cat{NP}_{nb}:\mathrm{room}_{0.0}}\inference[>]{\inference{\token{are}}{(\cat{S}_{dcl} \bsl \cat{NP}_{})/\cat{PP}_{}:\lambda x.\lambda y.\mathrm{be}_{0.0}^{0}(x, y)}\inference[>]{\inference{\token{in}}{\cat{PP}_{}/\cat{NP}_{}:\lambda x.\mathrm{in}_{0.0}^{0}(x)}\inference{\inference[>]{\inference[>]{\inference{\token{pretty}}{(\cat{N}_{}/\cat{N}_{})/(\cat{N}_{}/\cat{N}_{}):\lambda x.(x_{\bullet 3.526497e-2})}\inference{\token{shabby}}{\cat{N}_{}/\cat{N}_{}:\lambda x.(x_{\circ -25.0})}}{\cat{N}_{}/\cat{N}_{}:\lambda x.(x_{\circ -25.0}_{\bullet 3.526497e-2})}\inference{\token{condition}}{\cat{N}_{}:\mathrm{condition}_{0.0}}}{\cat{N}_{}:\mathrm{condition}_{-0.8816242}}}{\cat{NP}_{}:\mathrm{condition}_{-0.8816242}}}{\cat{PP}_{}:\mathrm{in}_{0.0}^{0}(\mathrm{condition}_{-0.8816242})}}{\cat{S}_{dcl} \bsl \cat{NP}_{}:\lambda y.\mathrm{be}_{0.0}^{0}(\mathrm{in}_{0.0}^{0}(\mathrm{condition}_{-0.8816242}), y)}}{\cat{S}_{dcl}:\mathrm{be}_{0.0}^{0}(\mathrm{in}_{0.0}^{0}(\mathrm{condition}_{-0.8816242}), \mathrm{room}_{0.0})}\inference[>]{\inference{\token{,}}{((\cat{S}_{dcl} \bsl \cat{S}_{dcl}) \bsl (\cat{S}_{dcl} \bsl \cat{S}_{dcl}))/(\cat{S}_{dcl} \bsl \cat{S}_{dcl}):\lambda x.\lambda y.\lambda z.(x\;z, y\;z)}\inference[>]{\inference{\token{but}}{(\cat{S}_{dcl} \bsl \cat{S}_{dcl})/\cat{S}_{dcl}:\lambda x.\lambda y.(x, y)}\inference[<]{\inference{\token{they}}{\cat{NP}_{}:\mathrm{they}_{0.0}}\inference[>]{\inference{\token{are}}{(\cat{S}_{dcl} \bsl \cat{NP}_{})/(\cat{S}_{adj} \bsl \cat{NP}_{}):\lambda x.x}\inference{\token{clean}}{\cat{S}_{adj} \bsl \cat{NP}_{}:\lambda x.(x_{\circ 45.0})}}{\cat{S}_{dcl} \bsl \cat{NP}_{}:\lambda x.(x_{\circ 45.0})}}{\cat{S}_{dcl}:\mathrm{they}_{45.0}}}{\cat{S}_{dcl} \bsl \cat{S}_{dcl}:\lambda y.(\mathrm{they}_{45.0}, y)}}{\cat{S}_{dcl} \bsl \cat{S}_{dcl}:\lambda y.\lambda z.(\mathrm{they}_{45.0}, z, y\;z)}}{\cat{S}_{dcl}:\lambda z.(\mathrm{they}_{45.0}, z, \mathrm{be}_{0.0}^{0}(\mathrm{in}_{0.0}^{0}(\mathrm{condition}_{-0.8816242}), \mathrm{room}_{0.0})\;z)}\inference{\token{.}}{\cat{S}_{dcl} \bsl \cat{S}_{dcl}:\lambda x.x}}{\cat{S}_{dcl}:\lambda z.(\mathrm{they}_{45.0}, z, \mathrm{be}_{0.0}^{0}(\mathrm{in}_{0.0}^{0}(\mathrm{condition}_{-0.8816242}), \mathrm{room}_{0.0})\;z)}
$$}
