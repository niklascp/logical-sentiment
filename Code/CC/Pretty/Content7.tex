\newcommand{\content}{$$
\inference[<]{\inference[<]{\inference[>]{\inference{\token{The}}{\cat{NP}_{nb}/\cat{N}_{}:\lambda x.x}\inference{\token{rooms}}{\cat{N}_{}:\mathrm{room}_{0.0}}}{\cat{NP}_{nb}:\mathrm{room}_{0.0}}\inference[>]{\inference[<B_\times]{\inference{\token{are}}{(\cat{S}_{dcl} \bsl \cat{NP}_{})/\cat{NP}_{}:\lambda x.\lambda y.\mathrm{be}_{0.0}^{0}(x, y)}\inference{\token{not}}{(\cat{S}_{X} \bsl \cat{NP}_{}) \bsl (\cat{S}_{X} \bsl \cat{NP}_{}):\lambda x.(x_{\circ 0.0})}}{(\cat{S}_{dcl} \bsl \cat{NP}_{})/\cat{NP}_{}:\lambda x.\lambda y.\mathrm{be}_{0.0}^{0}(x, y)}\inference[<]{\inference{\token{all}}{\cat{NP}_{}:\mathrm{all}_{0.0}}\inference{\inference[>]{\inference{\token{that}}{(\cat{S}_{adj} \bsl \cat{NP}_{})/(\cat{S}_{adj} \bsl \cat{NP}_{}):\lambda x.\lambda y.\mathrm{that}_{0.0}^{0}(x\;y)}\inference{\token{big}}{\cat{S}_{adj} \bsl \cat{NP}_{}:\lambda x.(x_{\circ 0.0})}}{\cat{S}_{adj} \bsl \cat{NP}_{}:\lambda y.\mathrm{that}_{0.0}^{0}(y_{\circ 0.0})}}{\cat{NP}_{} \bsl \cat{NP}_{}:\lambda y.\mathrm{that}_{0.0}^{0}(y_{\circ 0.0})}}{\cat{NP}_{}:\mathrm{that}_{0.0}^{0}(\mathrm{all}_{0.0})}}{\cat{S}_{dcl} \bsl \cat{NP}_{}:\lambda y.\mathrm{be}_{0.0}^{0}(\mathrm{that}_{0.0}^{0}(\mathrm{all}_{0.0}), y)}}{\cat{S}_{dcl}:\mathrm{be}_{0.0}^{0}(\mathrm{that}_{0.0}^{0}(\mathrm{all}_{0.0}), \mathrm{room}_{0.0})}\inference{\token{.}}{\cat{S}_{dcl} \bsl \cat{S}_{dcl}:\lambda x.x}}{\cat{S}_{dcl}:\mathrm{be}_{0.0}^{0}(\mathrm{that}_{0.0}^{0}(\mathrm{all}_{0.0}), \mathrm{room}_{0.0})}
$$}
